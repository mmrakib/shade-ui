import { style } from '@vanilla-extract/css'

export const canvasDivStyle = style({
    backgroundColor: 'f0f0f0',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100vh',
    margin: 0,
    padding: 0,
})
